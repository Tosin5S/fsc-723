/*
*Input: A list of n data items, L
*Output: The maximum and minimum elements in L

*1. Initialize two variables, max and min, with the first element of L.
*2. For i = 2 to n:
    * If L[i] > max, then set max = L[i].
    * If L[i] < min, then set min = L[i].
*3. Return max and min.
*/

